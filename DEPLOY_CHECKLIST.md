# ✅ 部署检查清单

## 📋 推送到 GitHub 前

- [ ] 确认 `.gitignore` 已正确配置（已创建）
- [ ] 确认 `.env` 文件不会被提交（已在 .gitignore 中）
- [ ] 确认敏感文件已排除（API 密钥、私钥等）
- [ ] 确认大文件已排除（数据、模型文件）
- [ ] 检查是否有硬编码的密钥或路径

---

## 🖥️ 服务器部署前准备

### 1. 检查服务器环境
- [ ] Node.js 18+ 已安装：`node --version`
- [ ] npm 已安装：`npm --version`
- [ ] Git 已安装：`git --version`
- [ ] 有足够的磁盘空间（至少 2GB）

### 2. 克隆代码
```bash
git clone https://github.com/your-username/poly-auto-trading.git
cd poly-auto-trading
```

### 3. 安装依赖
```bash
cd node_bot
npm install
```

### 4. 构建项目
```bash
npm run build
# 或直接运行 start（会自动 build）
```

### 5. 配置环境变量
```bash
cp .env.example .env
nano .env  # 填入真实的 API 密钥
```

**必需配置：**
- [ ] `POLYMARKET_API_KEY`
- [ ] `POLYMARKET_API_SECRET`
- [ ] `POLYMARKET_PASSPHRASE`
- [ ] `PRIVATE_KEY`
- [ ] `WALLET_ADDRESS`
- [ ] `RPC_URL`（配置自己的，不要用默认值）

### 6. 准备数据文件
- [ ] `data/btc_1m.csv` 存在
- [ ] `data/btc_15m.csv` 存在

**如果不存在，需要采集：**
```bash
npm run collect-data 30
```

### 7. 准备模型文件
- [ ] `../model/model.onnx` 存在
- [ ] `../model/metadata.json` 存在

**如果不存在，需要训练或上传：**
```bash
# 在服务器上训练（需要 Python 环境）
cd ../python_model
python3 -m venv venv
source venv/bin/activate
pip install -r requirements.txt
python train.py
```

---

## 🚀 启动机器人

### 方式 1: 直接运行（测试用）
```bash
cd node_bot
NODE_ENV=production npm run start
```

### 方式 2: 使用 PM2（推荐）
```bash
# 安装 PM2
npm install -g pm2

# 启动
cd node_bot
pm2 start npm --name "btc-bot" -- run start
pm2 save
pm2 startup  # 设置开机自启
```

### 方式 3: 使用 systemd（Linux）
参考 `DEPLOY.md` 中的 systemd 配置

---

## ✅ 启动后验证

- [ ] 进程正在运行：`ps aux | grep node`
- [ ] 没有立即崩溃的错误
- [ ] 日志显示模型加载成功
- [ ] 日志显示价格源连接成功
- [ ] 日志显示主循环启动

**查看日志：**
```bash
# PM2
pm2 logs btc-bot

# 直接运行
tail -f logs/*.log
```

---

## ⚠️ 重要提醒

1. **生产环境部署前：**
   - ✅ 先用开发模式测试：`NODE_ENV=development npm run start`
   - ✅ 确认所有功能正常
   - ✅ 确认预测和决策逻辑正确
   - ✅ 先用小资金测试：`POSITION_SIZE_USD=10`

2. **安全：**
   - 🔒 不要将 `.env` 提交到 Git
   - 🔒 使用强密码保护服务器
   - 🔒 定期备份重要文件

3. **监控：**
   - 📊 设置日志监控
   - 📊 监控进程状态
   - 📊 监控资源使用（CPU、内存）

---

## 🔄 快速部署命令总结

```bash
# 在服务器上执行
git clone https://github.com/your-username/poly-auto-trading.git
cd poly-auto-trading/node_bot
npm install
cp .env.example .env
# 编辑 .env 填入真实配置
npm run start  # 会自动 build 然后运行
```

---

## ❓ 常见问题

### Q: start 命令会自动 build 吗？
**A:** 是的！我添加了 `prestart` 脚本，运行 `npm run start` 会自动先执行 `npm run build`。

### Q: 如果 build 失败怎么办？
**A:** 检查 TypeScript 错误：`npm run typecheck`，修复后重试。

### Q: 数据文件太大，Git 拒绝怎么办？
**A:** 数据文件已在 `.gitignore` 中，不会提交。需要在服务器上采集或手动上传。

### Q: 模型文件需要提交吗？
**A:** 不需要，已在 `.gitignore` 中。可以在服务器上训练或从本地上传。

---

祝部署顺利！🎉

